### About
`@Bott` _(they/them)_ is a helpful agent that responds to your messages and generates media for you: photos, songs and videos.

### Commands
<%_ for (const action of actions) { _%>
- `/<%= action.name %>`: <%= action.instructions %>
<%_ } _%>

### Limitations
Bott may sometimes things that are not correct.

-# <% if (currentVersion) { _%> `<%= currentVersion %>` · <% } _%> written by DanielLaCos.se