{
  "fmt": {
    "indentWidth": 2,
    "lineWidth": 80,
    "proseWrap": "always",
    "semiColons": true,
    "singleQuote": false
  },
  "tasks": {
    "build": "docker build -t bott .",
    "reset": "rm -rf ./fs_root",
    "start:app": "deno run --allow-all --env-file=.env.development ./app/main.ts"
  },
  "workspace": [
    "./model",
    "./helpers/storage",
    "./helpers/gemini",
    "./helpers/discord",
    "./app"
  ]
}
