{
  "fmt": {
    "indentWidth": 2,
    "lineWidth": 80,
    "proseWrap": "always",
    "semiColons": true,
    "singleQuote": false
  },
  "tasks": {
    "build": "docker build -t bott .",
    "reset": "rm -rf ./fs_root",
    "setup": "brew bundle && gcloud auth login",
    "start:dev": "deno run --allow-all --env-file=.env.development ./app/main.ts",
    "start": "TODO: docker container"
  },
  "workspace": [
    "./model",
    "./libraries/storage",
    "./libraries/task",
    "./libraries/gemini",
    "./libraries/discord",
    "./app"
  ]
}
