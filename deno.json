{
  "tasks": {
    "start": "docker build -t bott . && docker run -p 8080:8080 --env-file .env.staging bott",
    "deploy": "sh ./deploy.sh"
  },
  "workspace": [
    "./data",
    "./helpers/gemini",
    "./helpers/discord",
    "./app"
  ]
}
